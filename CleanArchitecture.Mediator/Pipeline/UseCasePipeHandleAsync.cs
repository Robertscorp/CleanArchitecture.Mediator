using System.Threading.Tasks;

namespace CleanArchitecture.Mediator.Pipeline
{

    /// <summary>
    /// A delegate that represents the HandleAsync method for a Pipe in the Use Case Pipeline.
    /// </summary>
    public delegate Task UseCasePipeHandleAsync();

}
